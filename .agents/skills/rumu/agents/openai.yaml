interface:
  display_name: "Rumu"
  short_description: "Repo-specific patterns and workflows for rumu"
  default_prompt: "Use the rumu repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true